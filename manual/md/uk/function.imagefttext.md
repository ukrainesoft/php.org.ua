- [«imageftbbox](function.imageftbbox.md)
- [imagegammacorrect »](function.imagegammacorrect.md)

- [PHP Manual](index.md)
- [Функції GD та функції для роботи із зображеннями](ref.image.md)
- Нанесення тексту на зображення за допомогою шрифтів FreeType 2

# imagefttext

(PHP 4 \>u003d 4.0.7, PHP 5, PHP 7, PHP 8)

imagefttext — Нанесення тексту на зображення за допомогою шрифтів FreeType
2

### Опис

**imagefttext**(
[GdImage](class.gdimage.md) `$image`,
float `$size`,
float `$angle`,
int `$x`,
int `$y`,
int `$color`,
string `$font_filename`,
string `$text`,
array `$options` u003d \[\]
): array\|false

> **Примітка**:
>
> До PHP 8.0.0 **imagefttext()** - це розширений варіант
> [imagettftext()](function.imagettftext.md), який додатково
> підтримує `extrainfo`. Починаючи з PHP 8.0.0,
> [imagettftext()](function.imagettftext.md) є псевдонімом
> **imagefttext()**.

### Список параметрів

`image`
Об'єкт [GdImage](class.gdimage.md), який повертається однією з функцій
створення зображень, наприклад, такий як
[imagecreatetruecolor()](function.imagecreatetruecolor.md).

`size`
Розмір шрифту у пунктах.

`angle`
Кут у градусах, 0 градусів означає розташування тексту зліва направо.
Позитивні значення означає поворот тексту проти годинникової стрілки.
Наприклад, текст повернутий на 90 градусів потрібно буде читати знизу
вгору.

`x`
Координати `x` та `y` визначають відправну точку для першого символу
текст (конкретно, лівий нижній кут символу). Тут є відмінність від
функції [imagestring()](function.imagestring.md), в якій `x` та `y`
визначають лівий верхній кут першого символу. Наприклад, "верхній лівий"
має координати 0,0.

`y`
y-координат. Це позиція базової лінії шрифту, у загальному випадку вона не
збігається із нижчою точкою у символі.

`col`
Індекс потрібного кольору тексту дивіться
[imagecolorexact()](function.imagecolorexact.md).

`font_filename`
Шлях до TrueType шрифту, який потрібно використовувати.

Залежно від версії GD бібліотеки *якщо `font_filename` не
починається з `/`, то в кінець назви файлу буде додано розширення
`.ttf`*, і бібліотека намагатиметься знайти цей файл за адресою,
визначеному в установках бібліотеки.

У більшості випадків розміщення файлів шрифтів у директорії скрипту
вирішує подібні проблеми увімкнення файлів.

` <?php// Задание переменной окружения для GDputenv('GDFONTPATHu003d' . realpath('.'));// Имя шрифта для использования (обратите внимание на отсутствие расширения .ttf)$font u003d 'SomeFont';?> `

`text`
Текст для вставлення зображення.

`options`
| Ключ | Тип | Значення |
|---------------|-------|------------------------- -------------------|
| `linespacing` | float | Визначає малювання нижнього підкреслення

**Можливі значення масиву `options`**

### Значення, що повертаються

Ця функція повертає масив, який визначає чотири точки рамки тексту.
Текст усередині цих кордонів починається з лівого нижнього кута і
повертається проти годинникової стрілки:

| | |
|-----|-----------------------------|
| 0 | нижня ліва x-координата
| 1 | нижня ліва y-координата
| 2 | нижня права x-координата
| 3 | нижня права y-координата
| 4 | верхня права x-координата
| 5 | верхня права y-координата
| 6 | верхня ліва x-координата
| 7 | верхня ліва y-координата

У разі виникнення помилки повертає **`false`**.

### Список змін

| Версія | Опис |
|--------|---------------------------------------- -------------------------------------------------- ---------|
| 8.0.0 | `image` тепер чекає екземпляр [GdImage](class.gdimage.md); раніше очікували ресурс (resource). |

### Приклади

**Приклад #1 Приклад використання **imagefttext()****

`<?php// Створення зображення 300x100$im u003d imagecreatetruecolor(300, 100);$red u003d imagecolorallocate($im, 0xFF, 0x00, 0x00);$black u003d 0 / Зробимо червоний фонimagefilledrectangle($im, 0, 0, 299, 99, $red);// Шлях к ttf файлу шрифта$font_file u003d './arial.ttf';/| $im, 13, 0, 105, 55, $black, $font_file, 'PHP Manual');// Виведення зображенняheader('Content-Type: image/png');imagepng($im);imagedestroy($im) ;?> `

### Примітки

> **Примітка**: Ця функція доступна лише у випадку, якщо PHP був
> скомпільований за допомогою freetype (**--with-freetype-diru003dDIR**)

### Дивіться також

- [imageftbbox()](function.imageftbbox.md) - Визначення меж
тексту, що виводиться шрифтом freetype2
- [imagettftext()](function.imagettftext.md) - Малювання тексту на
зображенні шрифтом TrueType
